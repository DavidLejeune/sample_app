<% provide(:title, @user.name) %>

<% flash.each do |message_type, message| %>
  <div class="callout success alert-<%= message_type %>"><%= message %></div>
<% end %>


<h1>
  <%= gravatar_for @user, {size: 200} %>
  <%= @user.name %>
</h1>
